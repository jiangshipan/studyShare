package com.jsp.studyShare.Configuration;import com.jsp.studyShare.interceptor.CorsInterceptor;import com.jsp.studyShare.interceptor.LoginRequiredInterceptor;import com.jsp.studyShare.interceptor.PassportInterceptor;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Component;import org.springframework.web.servlet.config.annotation.InterceptorRegistry;import org.springframework.web.servlet.config.annotation.WebMvcConfigurerAdapter;@Componentpublic class footmapWebConfiguration extends WebMvcConfigurerAdapter {    //判断用户是谁    @Autowired    PassportInterceptor passportInterceptor;    //判断用户是否有权限访问    @Autowired    LoginRequiredInterceptor loginRequiredInterceptor;    //跨域拦截器    @Autowired    CorsInterceptor corsInterceptor;    //注册拦截器    @Override    public void addInterceptors(InterceptorRegistry registry) {        //所有路径都需要拦截器        registry.addInterceptor(passportInterceptor);        //跨域拦截器        registry.addInterceptor(corsInterceptor);        //访问特定页面拦截器        registry.addInterceptor(loginRequiredInterceptor).addPathPatterns("/user/**").excludePathPatterns("/user/login","/user/reg")                                                        .addPathPatterns("/data/**")                                                        .addPathPatterns("/category/**");        super.addInterceptors(registry);    }}