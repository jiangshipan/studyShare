package com.jsp.studyShare.controller;import com.jsp.studyShare.Service.LikeService;import com.jsp.studyShare.Service.TopicService;import com.jsp.studyShare.model.HostHolder;import com.jsp.studyShare.model.Response;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestMethod;import org.springframework.web.bind.annotation.RequestParam;import org.springframework.web.bind.annotation.ResponseBody;import java.util.Map;@Controllerpublic class LikeController {    @Autowired    private LikeService likeService;    @Autowired    private HostHolder hostHolder;    @Autowired    private TopicService topicService;    /**     * 更新点赞数     * @param tid     * @return     */    @RequestMapping(path = {"/like"}, method = {RequestMethod.GET})    @ResponseBody    public Response like(@RequestParam("tid") int tid) {        Map<String, Long> likeMap = likeService.like(hostHolder.getUser().getOpenid(), tid);        int likeCount = likeMap.get("like").intValue();        int disLikeCount = likeMap.get("disLike").intValue();        if (likeCount < 0) {            likeCount = 0;        }        if (disLikeCount < 0) {            disLikeCount = 0;        }        //更新点赞数        topicService.updateLikeCount(tid, likeCount, disLikeCount);        return new Response(0, "赞成功", String.valueOf(likeMap));    }    /**     * tid: topic对应的id     * @param tid     * @return     */    @RequestMapping(path = {"/disLike"}, method = {RequestMethod.GET})    @ResponseBody    public Response disLike(@RequestParam("tid") int tid) {        Map<String, Long> likeMap  = likeService.disLike(hostHolder.getUser().getOpenid(), tid);        int likeCount = likeMap.get("like").intValue();        int disLikeCount = likeMap.get("disLike").intValue();        if (likeCount < 0) {            likeCount = 0;        }        if (disLikeCount < 0) {            disLikeCount = 0;        }        //更新点赞数        topicService.updateLikeCount(tid, likeCount, disLikeCount);        return new Response(0, "踩成功", String.valueOf(likeMap));    }}